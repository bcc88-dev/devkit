def test_hello():
    from {{ project_slug }}.main import app
    from typer.testing import CliRunner

    runner = CliRunner()
    result = runner.invoke(app, ["hello", "devkit"])
    assert result.exit_code == 0
    assert "Hello, devkit!" in result.stdout
